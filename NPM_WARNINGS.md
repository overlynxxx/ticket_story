# 📝 Предупреждения npm

## Предупреждение о `@types/axios`

При установке зависимостей может появляться предупреждение:

```
npm warn deprecated @types/axios@0.14.4: This is a stub types definition. 
axios provides its own type definitions, so you do not need this installed.
```

### ✅ Это не критично!

**Почему появляется:**
- Пакет `@types/axios` установлен как зависимость библиотеки `@a2seven/yoo-checkout`
- Это не наша прямая зависимость, мы не можем её контролировать
- Библиотека `@a2seven/yoo-checkout` уже на последней версии (1.1.4)

**Влияет ли на работу:**
- ❌ Нет, это только предупреждение
- ✅ Приложение работает нормально
- ✅ Это не ошибка, а информационное сообщение

### 🔇 Как скрыть предупреждение (опционально):

1. **Использовать скрипт установки:**
   ```bash
   npm run install:clean
   ```

2. **Или игнорировать предупреждение:**
   - Просто продолжайте работу, предупреждение можно игнорировать
   - Оно не влияет на функциональность

3. **Или установить с флагом:**
   ```bash
   npm install --silent 2>&1 | grep -v "deprecated @types/axios"
   ```

### 📌 Рекомендация:

**Можно безопасно игнорировать это предупреждение.** Оно появится только при установке зависимостей и не влияет на работу приложения.

---

*Последнее обновление: 2026-01-13*
