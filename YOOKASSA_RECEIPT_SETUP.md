# 📄 Настройка фискальных чеков от ЮКассы (54-ФЗ)

## Что нужно сделать

Для получения фискальных чеков от ЮКассы необходимо выполнить несколько шагов в личном кабинете ЮКассы и настроить переменные окружения.

---

## Шаг 1: Настройка онлайн-кассы в ЮКассе

### 1.1. Войдите в личный кабинет ЮКассы
1. Откройте [https://yookassa.ru](https://yookassa.ru)
2. Войдите в личный кабинет
3. Перейдите в раздел **Настройки** → **Онлайн-касса**

### 1.2. Подключите онлайн-кассу
1. Если у вас еще нет онлайн-кассы, вам нужно:
   - Зарегистрировать онлайн-кассу в ФНС (Федеральная налоговая служба)
   - Или использовать облачную онлайн-кассу (например, от ЮКассы или других провайдеров)

2. В личном кабинете ЮКассы:
   - Нажмите **"Подключить онлайн-кассу"**
   - Следуйте инструкциям для подключения вашей кассы

### 1.3. Настройте ОФД (Оператор фискальных данных)
1. Выберите ОФД (например, "Такском", "Первый ОФД", "Астрал ОФД")
2. Убедитесь, что ОФД подключен и работает

---

## Шаг 2: Настройка отправки чеков в ЮКассе

### 2.1. Включите отправку чеков на email
1. В личном кабинете ЮКассы перейдите в **Настройки** → **Чеки**
2. Включите опцию **"Отправлять чеки покупателям на email"**
3. Убедитесь, что указан корректный email для получения уведомлений

### 2.2. Настройте параметры чеков
1. Укажите систему налогообложения (ОСН, УСН, ЕНВД и т.д.)
2. Настройте ставки НДС (если применимо)
3. Укажите наименование товара/услуги по умолчанию

---

## Шаг 3: Настройка переменных окружения в Vercel

### 3.1. Добавьте переменную `YOOKASSA_RECEIPT_ENABLED`
1. Откройте [Vercel Dashboard](https://vercel.com/dashboard)
2. Выберите проект `ticket_story`
3. Перейдите в **Settings** → **Environment Variables**
4. Нажмите **Add New**
5. Добавьте переменную:
   - **Key**: `YOOKASSA_RECEIPT_ENABLED`
   - **Value**: `true`
   - **Environment**: выберите `Production`, `Preview`, `Development`
6. Нажмите **Save**

### 3.2. Перезапустите деплой
1. Перейдите в **Deployments**
2. Найдите последний деплой
3. Нажмите на три точки (⋮) → **Redeploy**

Или сделайте новый коммит и пуш в `main` — Vercel автоматически создаст новый деплой.

---

## Шаг 4: Проверка работы

### 4.1. Тестовый платеж
1. Создайте тестовый платеж через ваше приложение
2. После успешной оплаты проверьте email покупателя
3. Должен прийти фискальный чек от ЮКассы

### 4.2. Проверка в логах Vercel
1. Перейдите в **Deployments** → выберите деплой → **Functions** → `api/create-payment`
2. В логах должно быть сообщение:
   ```
   Фискальный чек будет отправлен через ЮКассу на email: ...
   ```

---

## ⚠️ Важные замечания

### Для тестового магазина
- Фискальные чеки могут **не работать** в тестовом режиме
- Используйте **боевой магазин** для проверки работы чеков
- В тестовом режиме чеки могут не отправляться на email

### Параметры чека
В коде используются следующие параметры для фискального чека:

```javascript
vat_code: 1, // Без НДС (код 1)
payment_subject: 'service', // Услуга
payment_mode: 'full_payment', // Полный расчет
measure: 'piece', // Штука, единица товара
settlements: [{ type: 'cashless' }], // Безналичный расчет
timezone: 2 // Москва, Санкт-Петербург (UTC+3, MCK)
```

#### Коды НДС:
- `1` — Без НДС (используется в коде)
- `2` — НДС по ставке 0%
- `3` — НДС по ставке 10%
- `4` — НДС по ставке 20%
- `5` — НДС по расчетной ставке 10/110
- `6` — НДС по расчетной ставке 20/120
- `11` — НДС по ставке 22% (с 1 января 2026)
- `12` — НДС по расчетной ставке 22/122 (с 1 января 2026)

#### Признак предмета расчета:
- `service` — Услуга (используется в коде)
- `commodity` — Товар
- `job` — Работа
- и другие (см. [документацию](https://yookassa.ru/developers/payment-acceptance/receipts/54fz/yoomoney/parameters-values))

#### Признак способа расчета:
- `full_payment` — Полный расчет (используется в коде)
- `full_prepayment` — Полная предоплата

#### Мера количества:
- `piece` — Штука, единица товара (используется в коде)
- `gram`, `kilogram`, `meter` и другие (см. [документацию](https://yookassa.ru/developers/payment-acceptance/receipts/54fz/yoomoney/parameters-values))

#### Тип расчета:
- `cashless` — Безналичный расчет (используется в коде)
- `prepayment` — Предоплата (аванс)
- `postpayment` — Постоплата (кредит)

#### Часовые зоны:
- `2` — Москва, Санкт-Петербург, Нижний Новгород (UTC+3, MCK) — используется в коде
- `1` — Калининград (UTC+2)
- `3` — Астрахань, Самара (UTC+4)
- и другие (см. [документацию](https://yookassa.ru/developers/payment-acceptance/receipts/54fz/yoomoney/parameters-values))

### Если чеки не приходят
1. Проверьте, что онлайн-касса подключена и работает
2. Убедитесь, что ОФД подключен
3. Проверьте, что в ЮКассе включена отправка чеков на email
4. Проверьте, что переменная `YOOKASSA_RECEIPT_ENABLED=true` установлена в Vercel
5. Проверьте логи Vercel на наличие ошибок
6. Убедитесь, что используете **боевой магазин**, а не тестовый

---

## 📚 Дополнительная информация

- [Документация ЮКассы: Фискальные чеки](https://yookassa.ru/developers/payment-acceptance/receipts)
- [Документация ЮКассы: Настройка онлайн-кассы](https://yookassa.ru/developers/using-api/receipts)
- [Документация Vercel: Environment Variables](https://vercel.com/docs/concepts/projects/environment-variables)

---

## Текущая настройка в коде

В файле `api/create-payment.js` уже настроена отправка фискальных чеков:

```javascript
const receipt = process.env.YOOKASSA_RECEIPT_ENABLED === 'true' ? {
  customer: {
    email: email.trim()
  },
  items: [
    {
      description: `Билеты: ${event.name} - ${category.name}`,
      quantity: quantity.toString(),
      amount: {
        value: amount.toFixed(2),
        currency: 'RUB'
      },
      vat_code: 1 // НДС 20% - измените при необходимости
    }
  ]
} : undefined;
```

Если `YOOKASSA_RECEIPT_ENABLED=true`, то при создании платежа будет отправлен фискальный чек на email покупателя через ЮКассу.

---

## Альтернатива: Информационные чеки через Resend

Если фискальные чеки от ЮКассы не настроены, система автоматически отправляет **информационные чеки** через Resend API. Эти чеки не являются фискальными документами, но содержат всю информацию о платеже.

Для работы информационных чеков нужно:
- Настроить `RESEND_API_KEY` в Vercel
- Настроить `EMAIL_FROM` в Vercel

Информационные чеки отправляются автоматически при успешной оплате, если в метаданных платежа указан `sendReceipt: 'true'`.
