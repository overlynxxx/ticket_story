# 🌐 Деплой на Vercel через веб-интерфейс

## Пошаговая инструкция

### Шаг 1: Регистрация/Вход

1. Перейдите на [vercel.com](https://vercel.com)
2. Нажмите **"Sign Up"** или **"Log In"**
3. Войдите через GitHub (рекомендуется)

### Шаг 2: Создание проекта

1. После входа нажмите **"Add New..."** → **"Project"**
2. Выберите ваш репозиторий `ticket_story`
3. Если репозитория нет в списке:
   - Нажмите **"Adjust GitHub App Permissions"**
   - Выберите репозиторий `ticket_story`
   - Нажмите **"Save"**

### Шаг 3: Настройка проекта

В настройках проекта:

1. **Framework Preset:** Оставьте **"Other"** или выберите **"Vite"**
2. **Root Directory:** Оставьте `./` (корень проекта)
3. **Build Command:** `npm run build`
4. **Output Directory:** `dist`
5. **Install Command:** `npm install`

### Шаг 4: Добавление переменных окружения

**ВАЖНО:** Сделайте это ДО первого деплоя!

1. В настройках проекта найдите раздел **"Environment Variables"**
2. Нажмите **"Add"** и добавьте:

   **Первая переменная:**
   - **Name:** `YOOKASSA_SHOP_ID`
   - **Value:** `1248098`
   - **Environment:** Выберите все (Production, Preview, Development)
   - Нажмите **"Save"**

   **Вторая переменная:**
   - **Name:** `YOOKASSA_SECRET_KEY`
   - **Value:** `test_44nfjs8TvfyAWb77UlYIUU5kGUB28f-gITBPdKVyKpE`
   - **Environment:** Выберите все (Production, Preview, Development)
   - Нажмите **"Save"**

   **Третья переменная (опционально):**
   - **Name:** `RETURN_URL`
   - **Value:** `https://your-app.vercel.app/payment-success`
   - **Environment:** Production
   - Нажмите **"Save"**

### Шаг 5: Деплой

1. После настройки нажмите **"Deploy"**
2. Дождитесь завершения сборки (обычно 1-2 минуты)
3. После успешного деплоя вы получите URL:
   - Например: `https://ticket-story-abc123.vercel.app`

### Шаг 6: Настройка фронтенда

После получения URL вашего проекта:

1. Создайте файл `.env` в корне проекта:
```
REACT_APP_API_URL=https://your-app.vercel.app
```

2. Или обновите `src/pages/Payment.jsx`:
```javascript
const API_URL = 'https://your-app.vercel.app'
```

3. Пересоберите и задеплойте фронтенд

### Шаг 7: Настройка Webhook в ЮКассе

1. Зайдите в личный кабинет [ЮКассы](https://yookassa.ru)
2. Перейдите в **Настройки магазина**
3. Найдите раздел **"Webhook"**
4. URL webhook: `https://your-app.vercel.app/api/payment-webhook`
5. Сохраните настройки

## Автоматический деплой

После первого деплоя Vercel будет автоматически деплоить при каждом push в:
- **main** ветку → Production
- **dev** ветку → Preview

## Проверка работы

После деплоя проверьте:

1. **Health check:**
   ```
   https://your-app.vercel.app/api/health
   ```
   Должен вернуть: `{"success": true, "status": "ok"}`

2. **Список мероприятий:**
   ```
   https://your-app.vercel.app/api/events
   ```

## Полезные ссылки

- [Vercel Dashboard](https://vercel.com/dashboard)
- [Документация Vercel](https://vercel.com/docs)
- [Environment Variables в Vercel](https://vercel.com/docs/concepts/projects/environment-variables)

## Решение проблем

### Ошибка при деплое:
- Проверьте, что все зависимости установлены
- Убедитесь, что переменные окружения добавлены
- Проверьте логи деплоя в Vercel Dashboard

### API не работает:
- Проверьте, что файлы в папке `api/` на месте
- Убедитесь, что переменные окружения установлены
- Проверьте логи функций в Vercel Dashboard → Functions

### CORS ошибки:
- Vercel автоматически настраивает CORS для API
- Если проблемы, проверьте заголовки в ответах API
