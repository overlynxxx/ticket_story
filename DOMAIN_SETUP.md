# 🌐 Настройка домена Namecheap для Vercel

## Шаг 1: Добавление домена в Vercel

1. Откройте [Vercel Dashboard](https://vercel.com/dashboard)
2. Выберите ваш проект `ticket-story`
3. Перейдите в **Settings** → **Domains**
4. Нажмите **"Add Domain"**
5. Введите ваш домен (например: `ticket-story.com` или `www.ticket-story.com`)
6. Нажмите **"Add"**

## Шаг 2: Получение DNS записей от Vercel

После добавления домена Vercel покажет вам необходимые DNS записи:

### Для корневого домена (example.com):
- **Type:** `A`
- **Name:** `@` (или оставьте пустым)
- **Value:** IP адрес от Vercel (например: `76.76.21.21`)

### Для поддомена www (www.example.com):
- **Type:** `CNAME`
- **Name:** `www`
- **Value:** `cname.vercel-dns.com.` (или другой адрес от Vercel)

**Важно:** Vercel покажет точные значения для вашего домена. Скопируйте их!

## Шаг 3: Настройка DNS в Namecheap

### Вариант 1: Использование DNS Namecheap (рекомендуется)

1. Войдите в [Namecheap](https://www.namecheap.com/)
2. Перейдите в **Domain List**
3. Нажмите **"Manage"** рядом с вашим доменом
4. В разделе **"Nameservers"** выберите **"Namecheap BasicDNS"** (или оставьте как есть)
5. Перейдите в раздел **"Advanced DNS"**
6. Удалите все существующие записи (или оставьте только нужные)
7. Добавьте новые записи:

#### Для корневого домена:
- Нажмите **"Add New Record"**
- Выберите тип **"A Record"**
- **Host:** `@` (или оставьте пустым)
- **Value:** IP адрес от Vercel (скопируйте из Vercel Dashboard)
- **TTL:** `Automatic` (или `30 min`)
- Нажмите **"Save"**

#### Для поддомена www:
- Нажмите **"Add New Record"**
- Выберите тип **"CNAME Record"**
- **Host:** `www`
- **Value:** `cname.vercel-dns.com.` (или значение от Vercel)
- **TTL:** `Automatic` (или `30 min`)
- Нажмите **"Save"**

### Вариант 2: Использование DNS Vercel (альтернатива)

1. В Namecheap → **Domain List** → **Manage**
2. В разделе **"Nameservers"** выберите **"Custom DNS"**
3. Введите nameservers от Vercel (они будут показаны в Vercel Dashboard):
   - `ns1.vercel-dns.com`
   - `ns2.vercel-dns.com`
4. Нажмите **"Save"**

## Шаг 4: Ожидание распространения DNS

- Обычно DNS изменения вступают в силу через **15-30 минут**
- Может занять до **24-48 часов** (максимум)
- Проверить можно через [whatsmydns.net](https://www.whatsmydns.net/)

## Шаг 5: Проверка настройки

1. В Vercel Dashboard → **Settings** → **Domains**
2. Дождитесь статуса **"Valid Configuration"** (зеленая галочка)
3. Если статус **"Invalid Configuration"**, проверьте DNS записи

### Проверка через терминал:
```bash
# Проверка A записи для корневого домена
dig example.com A

# Проверка CNAME для www
dig www.example.com CNAME

# Или используйте nslookup
nslookup example.com
```

## Шаг 6: Настройка SSL (автоматически)

Vercel автоматически выдаст SSL сертификат (Let's Encrypt) после успешной настройки DNS:
- Обычно занимает **5-10 минут**
- Статус можно проверить в **Settings** → **Domains**

## 🔧 Решение проблем

### Домен не работает после настройки:

1. **Проверьте DNS записи:**
   - Убедитесь, что IP адрес A записи правильный
   - Убедитесь, что CNAME запись указывает на правильный адрес Vercel

2. **Очистите DNS кэш:**
   ```bash
   # Windows
   ipconfig /flushdns
   
   # macOS/Linux
   sudo dscacheutil -flushcache
   sudo killall -HUP mDNSResponder
   ```

3. **Проверьте через онлайн инструменты:**
   - [whatsmydns.net](https://www.whatsmydns.net/)
   - [dnschecker.org](https://dnschecker.org/)

### "Invalid Configuration" в Vercel:

- Убедитесь, что DNS записи добавлены правильно
- Подождите 15-30 минут для распространения
- Проверьте, что нет конфликтующих записей

### Домен работает, но показывает старую версию:

- Очистите кэш браузера (Ctrl+Shift+Delete)
- Проверьте, что проект задеплоен на Vercel
- Убедитесь, что домен привязан к правильному проекту

## 📝 Пример конфигурации DNS

Для домена `ticket-story.com`:

```
Type    Host    Value                    TTL
A       @       76.76.21.21              Automatic
CNAME   www     cname.vercel-dns.com.    Automatic
```

## ✅ Чеклист

- [ ] Домен добавлен в Vercel Dashboard
- [ ] DNS записи скопированы из Vercel
- [ ] A запись добавлена в Namecheap для корневого домена
- [ ] CNAME запись добавлена в Namecheap для www
- [ ] Прошло минимум 15-30 минут
- [ ] Статус в Vercel показывает "Valid Configuration"
- [ ] SSL сертификат выдан (автоматически)
- [ ] Домен открывается в браузере

## 🔗 Полезные ссылки

- [Vercel DNS Documentation](https://vercel.com/docs/concepts/projects/domains)
- [Namecheap DNS Management](https://www.namecheap.com/support/knowledgebase/article.aspx/767/10/how-to-change-dns-for-a-domain/)
- [DNS Propagation Checker](https://www.whatsmydns.net/)

---

**Примечание:** После настройки домена обновите `return_url` в настройках YooKassa, чтобы использовать ваш домен `tupik.xyz`.
