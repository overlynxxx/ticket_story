# 🚀 Ручной деплой на Vercel

Если Vercel не видит новую версию автоматически, можно задеплоить вручную.

## Вариант 1: Через Vercel Dashboard (Рекомендуется)

1. Зайдите на [vercel.com](https://vercel.com)
2. Выберите ваш проект `ticket-story`
3. Нажмите **"Deployments"** в меню
4. Нажмите **"Redeploy"** на последнем деплое
5. Или нажмите **"Deploy"** → **"Deploy Latest Commit"**

## Вариант 2: Через Vercel CLI

```bash
# Установите Vercel CLI (если еще не установлен)
npm install -g vercel

# Войдите в Vercel
vercel login

# Перейдите в папку проекта
cd /Users/user/Documents/ticket_story

# Задеплойте
vercel --prod
```

## Вариант 3: Проверка подключения GitHub

1. Зайдите на [vercel.com](https://vercel.com)
2. Выберите проект → **Settings** → **Git**
3. Убедитесь, что:
   - Репозиторий подключен: `overlynxxx/ticket_story`
   - Production Branch: `main`
   - Auto-deploy: включен

## Вариант 4: Принудительный деплой через GitHub

Если нужно запустить деплой через GitHub Actions:

1. Зайдите на [github.com](https://github.com/overlynxxx/ticket_story)
2. Перейдите в **Actions**
3. Выберите workflow **"Deploy to Vercel (Production)"**
4. Нажмите **"Run workflow"**
5. Выберите ветку `main` и нажмите **"Run workflow"**

## Проверка деплоя

После деплоя проверьте:
- URL: `https://ticket-story.vercel.app`
- Логи: Vercel Dashboard → Deployments → выберите деплой → Logs

## Если деплой не работает

1. Проверьте логи в Vercel Dashboard
2. Убедитесь, что переменные окружения настроены:
   - `YOOKASSA_SHOP_ID`
   - `YOOKASSA_SECRET_KEY`
3. Проверьте, что `package.json` содержит все зависимости
4. Убедитесь, что `vercel.json` настроен правильно

---

**Текущая версия в main:** Проверьте последний коммит в ветке `main`
