# Telegram Mini App - Продажа билетов

Веб-приложение для продажи билетов через Telegram Mini App с интеграцией ЮКассы и генерацией QR-кодов.

## Функционал

- ✅ Выбор категории билетов (Эконом, Стандарт, VIP)
- ✅ Настройка категорий через конфиг файл
- ✅ Оплата через ЮКассу с QR-кодом
- ✅ Генерация QR-кода билета после оплаты
- ✅ Адаптивный дизайн для мобильных устройств
- ✅ Интеграция с Telegram Web App SDK

## Установка

```bash
npm install
```

## Настройка

### 1. Конфигурация билетов

Отредактируйте файл `config/tickets.json`:

```json
{
  "event": {
    "name": "Название мероприятия",
    "date": "2024-12-31",
    "time": "20:00",
    "venue": "Место проведения"
  },
  "ticketCategories": [
    {
      "id": "economy",
      "name": "Эконом",
      "price": 1000,
      "description": "Описание категории",
      "available": true
    }
  ],
  "yookassa": {
    "shopId": "YOUR_SHOP_ID",
    "secretKey": "YOUR_SECRET_KEY"
  }
}
```

### 2. Настройка ЮКассы

1. Зарегистрируйтесь на [ЮКасса](https://yookassa.ru/)
2. Получите `shopId` и `secretKey`
3. Добавьте их в `config/tickets.json`

**Важно:** В продакшене конфиг с ключами должен храниться на бэкенде!

## Разработка

```bash
npm run dev
```

Приложение будет доступно по адресу `http://localhost:3000`

## Сборка для продакшена

```bash
npm run build
```

Собранные файлы будут в папке `dist`

## Структура проекта

```
ticket_story/
├── config/
│   └── tickets.json          # Конфигурация билетов
├── src/
│   ├── pages/
│   │   ├── Home.jsx          # Главная страница
│   │   ├── TicketSelection.jsx  # Выбор количества билетов
│   │   ├── Payment.jsx       # Страница оплаты
│   │   └── TicketView.jsx    # Просмотр билета с QR
│   ├── App.jsx               # Главный компонент
│   ├── main.jsx              # Точка входа
│   └── index.css             # Глобальные стили
├── index.html
├── package.json
└── vite.config.js
```

## Интеграция с Telegram

Приложение автоматически определяет, запущено ли оно в Telegram, и использует Telegram Web App SDK для:
- Расширения на весь экран
- Использования цветовой схемы Telegram
- Использования кнопок Telegram (MainButton, BackButton)

## Интеграция с ЮКассой

**Текущая версия содержит демо-реализацию.** Для продакшена необходимо:

1. Создать бэкенд API для создания платежей
2. Реализовать webhook для обработки уведомлений от ЮКассы
3. Сохранять билеты в базе данных
4. Проверять статус оплаты перед выдачей билета

📖 **Подробная инструкция:** См. файл [INTEGRATION.md](./INTEGRATION.md)

💻 **Пример бэкенд кода:** См. файл [backend-example.js](./backend-example.js)

Пример структуры бэкенда:

```javascript
// POST /api/create-payment
{
  "amount": 1000,
  "ticketCategory": "economy",
  "quantity": 1
}

// Webhook от ЮКассы
POST /api/payment-webhook
{
  "event": "payment.succeeded",
  "object": { ... }
}
```

## Безопасность

⚠️ **Важно:**
- Не храните секретные ключи ЮКассы в клиентском коде
- Все платежи должны проходить через ваш бэкенд
- Валидируйте платежи на сервере перед выдачей билетов
- Используйте HTTPS в продакшене

## Лицензия

MIT
