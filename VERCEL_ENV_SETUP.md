# 🔧 Настройка переменных окружения в Vercel

## Добавление переменных окружения

### Шаг 1: Откройте настройки проекта
1. Войдите в [Vercel Dashboard](https://vercel.com/dashboard)
2. Выберите ваш проект `ticket_story`
3. Перейдите в раздел **Settings** (Настройки)
4. В левом меню выберите **Environment Variables** (Переменные окружения)

### Шаг 2: Добавьте переменные

Добавьте следующие переменные окружения:

#### Обязательные переменные:

1. **YOOKASSA_SHOP_ID**
   - Значение: `1248098` (или ваш Shop ID)
   - Environment: `Production`, `Preview`, `Development`

2. **YOOKASSA_SECRET_KEY**
   - Значение: ваш Secret Key от ЮКассы (начинается с `test_` для тестового или `live_` для боевого)
   - Environment: `Production`, `Preview`, `Development`

3. **RESEND_API_KEY**
   - Значение: ваш API ключ от Resend
   - Environment: `Production`, `Preview`, `Development`

4. **EMAIL_FROM**
   - Значение: `Tickets <noreply@yourdomain.com>` (замените на ваш домен)
   - Environment: `Production`, `Preview`, `Development`

#### Опциональные переменные:

5. **YOOKASSA_RECEIPT_ENABLED** (для фискальных чеков)
   - Значение: `true` (для включения) или не добавляйте (для отключения)
   - Environment: `Production`, `Preview`, `Development`
   - ⚠️ **Важно**: Включайте только если у вас настроена онлайн-касса и ОФД в ЮКассе!

6. **RETURN_URL** (опционально)
   - Значение: `https://tupik.xyz/payment-success`
   - Environment: `Production`
   - Если не указано, используется автоматическое определение URL (будет использован домен `tupik.xyz`)

### Шаг 3: Как добавить переменную

1. Нажмите кнопку **Add New** (Добавить новую)
2. В поле **Key** введите название переменной (например, `YOOKASSA_RECEIPT_ENABLED`)
3. В поле **Value** введите значение (например, `true`)
4. Выберите окружения, где будет доступна переменная:
   - ✅ **Production** — для продакшена
   - ✅ **Preview** — для preview-деплоев
   - ✅ **Development** — для локальной разработки
5. Нажмите **Save** (Сохранить)

### Шаг 4: Перезапуск деплоя

После добавления переменных окружения:
1. Перейдите в раздел **Deployments** (Деплои)
2. Найдите последний деплой
3. Нажмите на три точки (⋮) справа от деплоя
4. Выберите **Redeploy** (Переразвернуть)
5. Подтвердите переразвертывание

Или просто сделайте новый коммит и пуш в `main` — Vercel автоматически создаст новый деплой с обновленными переменными.

## 📋 Полный список переменных для копирования

```
YOOKASSA_SHOP_ID=1248098
YOOKASSA_SECRET_KEY=your_secret_key_here
RESEND_API_KEY=your_resend_api_key_here
EMAIL_FROM=Tickets <noreply@yourdomain.com>
YOOKASSA_RECEIPT_ENABLED=true
```

## ⚠️ Важные замечания

1. **Безопасность**: Никогда не коммитьте переменные окружения в Git!
2. **Фискальные чеки**: `YOOKASSA_RECEIPT_ENABLED=true` работает только если:
   - У вас настроена онлайн-касса в ЮКассе
   - Подключен ОФД (Оператор фискальных данных)
   - В личном кабинете ЮКассы включена отправка чеков
3. **Тестирование**: Для тестового магазина фискальные чеки могут не работать. Используйте боевой магазин для проверки.

## 🔍 Проверка переменных

После добавления переменных вы можете проверить их в логах Vercel:
1. Перейдите в **Deployments** → выберите деплой → **Functions** → выберите функцию
2. В логах вы увидите сообщения о том, какие переменные загружены

## 📚 Дополнительная информация

- [Документация Vercel: Environment Variables](https://vercel.com/docs/concepts/projects/environment-variables)
- [Документация ЮКассы: Фискальные чеки](https://yookassa.ru/developers/payment-acceptance/receipts)
