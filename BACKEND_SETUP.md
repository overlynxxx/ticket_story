# 🚀 Быстрый запуск бэкенда

## Установка и запуск

```bash
# Перейдите в папку бэкенда
cd backend

# Установите зависимости
npm install

# Создайте файл .env (см. backend/ENV_SETUP.md)
# Или скопируйте из примера:
cp ENV_SETUP.md .env
# Затем отредактируйте .env

# Запустите сервер
npm run dev
```

Сервер будет доступен на `http://localhost:3001`

## Настройка фронтенда

Фронтенд автоматически подключится к бэкенду на `http://localhost:3001`

Если бэкенд на другом адресе, создайте файл `.env` в корне проекта:
```
REACT_APP_API_URL=http://your-backend-url:3001
```

## Проверка работы

1. Запустите бэкенд: `cd backend && npm run dev`
2. Запустите фронтенд: `npm run dev`
3. Попробуйте купить билет

## API Endpoints

- `GET /api/health` - проверка работоспособности
- `GET /api/events` - список мероприятий
- `POST /api/create-payment` - создание платежа
- `GET /api/payment/:id/status` - статус платежа
- `POST /api/payment-webhook` - webhook от ЮКассы
- `GET /api/ticket/:id` - информация о билете

Подробнее в `backend/README.md`
